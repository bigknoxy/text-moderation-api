services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - HF_API_KEY=${HF_API_KEY}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
  redis:
    image: redis:7
    ports:
      - "6379:6379"
